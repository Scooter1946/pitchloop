# Nexla lineage proof

Live proof is pending credentials. Save only redacted artifacts here:

- `nexla-flow.json`: flow, source, transformed Nexset, and destination IDs plus status;
- `nexla-lineage.png`: UI lineage/status screenshot;
- no service keys, webhook secrets, tokens, or raw authorization headers.

